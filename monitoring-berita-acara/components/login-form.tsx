"use client";
import { useState } from "react";
import { Loader2,LockKeyhole } from "lucide-react";
export function LoginForm(){const [loading,setLoading]=useState(false);const [error,setError]=useState("");
 async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError("");const data=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch("/api/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(data)});const body=await r.json();if(!r.ok){setError(body.message||"Login gagal");setLoading(false);return}location.href=body.redirect}
 return <form onSubmit={submit} className="card p-7"><div className="mb-6"><span className="brand-mark mb-4"><LockKeyhole size={20}/></span><h1 className="text-2xl font-black">Masuk ke sistem</h1><p className="muted text-sm">Gunakan akun internal atau operator Anda.</p></div>{error&&<div className="mb-4 rounded-xl bg-red-50 p-3 text-sm text-red-700">{error}</div>}<label className="mb-4 block"><span className="label">Username</span><input className="input" name="username" type="text" autoComplete="username" minLength={3} maxLength={30} required placeholder="Masukkan username"/></label><label className="mb-5 block"><span className="label">Password</span><input className="input" name="password" type="password" autoComplete="current-password" minLength={8} required/></label><button disabled={loading} className="btn btn-primary w-full">{loading?<><Loader2 className="animate-spin" size={17}/>Memeriksa...</>:"Masuk"}</button><p className="muted mt-5 text-xs">Mode demo tanpa database: admin / Admin123! atau internal / Internal123!</p></form>}
